<%
   if(!links){
       links = [];
   }
%>

<h3>revision demos:</h3>
<% links.forEach(function(link){ %>
 <a href="<%= link.href %>"><%= '' + link.name.replace(/-/g,'_') + '' || 'no name' %></a>  <br><br>
<% }); %>